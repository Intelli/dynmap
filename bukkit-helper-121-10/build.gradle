eclipse {
    project {
        name = "Dynmap(Paper-1.21.10)"
    }
}

description = 'bukkit-helper-1.21.10'

sourceCompatibility = targetCompatibility = compileJava.sourceCompatibility = compileJava.targetCompatibility = JavaLanguageVersion.of(21)

dependencies {
  implementation project(':bukkit-helper')
  implementation project(':dynmap-api')
  implementation project(path: ':DynmapCore', configuration: 'shadow')
  compileOnly('io.papermc.paper:paper-api:1.21.10-R0.1-SNAPSHOT')
  // Spigot server jar is required for NMS compile; Paper API is used for Bukkit types above.
  compileOnly('org.spigotmc:spigot:1.21.10-R0.1-SNAPSHOT') {
    exclude group: 'com.mojang', module: 'jtracy'
  }
}
